# ── 依赖 ─────────────────────────────────────────
node_modules/
.pnp/
.pnp.js

# ── 构建产物 / 类型缓存 ──────────────────────────
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# ── 本地环境变量（重要：含真实 API key，绝不提交） ──
.env
.env.local
.env.*.local
!.env.example

# ── 本地数据（README 说 PDF 可能有隐私） ─────────
data/documents/*
!data/documents/.gitkeep
data/indexes/*
!data/indexes/.gitkeep
data/app.json
data/app.json.*

# ── 备份 / 临时文件 ──────────────────────────────
*.bak
*.bak.*
*.tmp
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
*.swp
*.swo

# ── 编辑器 / IDE ────────────────────────────────
.idea/
.vscode/
*.iml

# ── Claude Code 机器本地设置（路径因机器而异） ───
.claude/settings.local.json

# ── 测试覆盖 ─────────────────────────────────────
coverage/
.nyc_output/

# ── Vercel ──────────────────────────────────────
.vercel/
